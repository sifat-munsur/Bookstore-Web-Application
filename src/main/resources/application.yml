#spring.application.name=bookstore
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/bookstore_db
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # auto create tables
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080